# 选课助手 UX 增强设计文档

日期：2026-01-13

## 概述

本次优化针对三个用户痛点：
1. 冲突不够直观
2. 课程太多难以筛选
3. 移动端体验差

---

## 一、冲突悬浮高亮

### 交互逻辑
- 鼠标移入优先级栏中的任意课程卡片时，检测该课程的所有冲突课程
- 所有冲突课程卡片添加高亮样式（红色边框 + 浅红背景 + 轻微脉冲动画）
- 同时在日历视图中，对应的冲突日期格子也高亮显示
- 鼠标移出后，恢复原状

### 视觉样式
```css
.course-card.conflict-highlight {
    border-color: #ef4444;
    background: #fef2f2;
    animation: pulse 0.8s ease-in-out infinite;
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.02); }
}
```

### 实现要点
- 复用现有的 `getConflicts(courseId)` 函数
- 给课程卡片添加 `mouseenter` 和 `mouseleave` 事件
- 通过 `data-id` 属性定位需要高亮的卡片

---

## 二、课程标签筛选

### 标签分类

| 标签 | 关键词匹配规则 |
|------|----------------|
| 金融 | 投融资、财富、证券、银行、金融、资本、保险 |
| AI/科技 | AI、人工智能、区块链、数字、数据、科技 |
| 战略 | 战略、商业模式、竞争、并购、公司治理 |
| 人文 | 历史、文化、诸子、葡萄酒、宝玉石、哲学 |
| 营销 | 品牌、消费、零售、电商、营销 |
| 领导力 | 沟通、团队、人才、领导、管理 |
| 线上 | is_online === true |
| 2学分 | credits > 1 |

### UI 布局
```
[搜索框________________] [月份▼]
[全部] [金融] [AI/科技] [战略] [人文] [营销] [领导力] | [线上] [2学分]
```

### 交互逻辑
- 默认选中「全部」，显示所有未选课程
- 点击某个标签，筛选出该类别课程，标签变为选中态（主题色背景）
- 支持多选：可同时选中「金融」+「线上」，显示交集
- 再次点击已选标签则取消选中
- 点击「全部」清除所有筛选

### CSS 样式
```css
.filter-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-bottom: 12px;
}

.filter-tag {
    padding: 4px 10px;
    border-radius: 16px;
    font-size: 0.75rem;
    border: 1px solid var(--gray-300);
    background: white;
    cursor: pointer;
    transition: all 0.2s;
}

.filter-tag:hover {
    border-color: var(--primary);
}

.filter-tag.active {
    background: var(--primary);
    color: white;
    border-color: var(--primary);
}
```

---

## 三、移动端底部标签页

### 触发条件
- 屏幕宽度 ≤ 700px 时启用移动端布局

### 布局结构
```
┌─────────────────────────┐
│  Header: 统计信息       │  ← 保持不变，数字缩小
├─────────────────────────┤
│                         │
│    主内容区域           │  ← 单面板显示，可滚动
│    (课程库/我的选课/日历)│
│                         │
├─────────────────────────┤
│  📚课程  ✅选课  📅日历  │  ← 底部固定导航
└─────────────────────────┘
```

### 底部导航样式
- 固定在底部，高度 56px
- 三个等宽按钮，图标 + 文字
- 当前激活项用主题色高亮
- 选课按钮上显示已选数量角标

### CSS 样式
```css
@media (max-width: 700px) {
    .main-container {
        grid-template-columns: 1fr;
        padding-bottom: 70px; /* 为底部导航留空间 */
    }

    .panel {
        display: none;
    }

    .panel.active {
        display: flex;
    }

    .bottom-nav {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        height: 56px;
        background: white;
        border-top: 1px solid var(--gray-200);
        display: flex;
        justify-content: space-around;
        align-items: center;
        z-index: 100;
    }

    .bottom-nav-item {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2px;
        padding: 8px 16px;
        color: var(--gray-500);
        font-size: 0.7rem;
        cursor: pointer;
        position: relative;
    }

    .bottom-nav-item.active {
        color: var(--primary);
    }

    .bottom-nav-item .icon {
        font-size: 1.2rem;
    }

    .bottom-nav-item .badge {
        position: absolute;
        top: 2px;
        right: 8px;
        background: var(--danger);
        color: white;
        font-size: 0.6rem;
        padding: 1px 5px;
        border-radius: 10px;
    }
}
```

### "我的选课"面板移动端优化
- 三个优先级栏改为垂直堆叠（必选 → 想选 → 可选）
- 每个栏可折叠/展开
- 添加课程改为：点击课程 → 弹出选择框「添加到 必选/想选/可选」
- 金币分配和智能分配按钮保留

### 日历面板移动端优化
- 每月日历改为单列滚动
- 日期格子加大，方便点击

---

## 实现顺序建议

1. **第一步：冲突悬浮高亮** - 改动最小，见效快
2. **第二步：标签筛选** - 中等工作量，提升明显
3. **第三步：移动端适配** - 工作量最大，需要重构部分布局

---

## 技术要点

- 保持零依赖的纯 JavaScript 实现
- 所有新增样式添加到现有 `<style>` 标签中
- 所有新增脚本添加到现有 `<script>` 标签中
- 保持 localStorage 数据结构不变，确保向后兼容
